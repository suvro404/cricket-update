const matchList = state => state.matchList;
const liveMatchList = state => state.liveMatchList;
const upcomingMatchList = state => state.upcomingMatchList;

export default {
    matchList,
    liveMatchList,
    upcomingMatchList
}
